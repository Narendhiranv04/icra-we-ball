# Claude entry point

Read [`CLAUDE_HANDOFF.md`](CLAUDE_HANDOFF.md) completely before modifying this
repository. It contains the current research intent, architecture boundaries,
Git/worktree status, experimental protocols, verified limitations, remote-model
setup, and prioritized next steps.

Also inspect the live worktree before acting:

```bash
git status --short --branch
git log -5 --oneline --decorate
```

Do not reset, clean, discard, or overwrite the existing dirty worktree.
