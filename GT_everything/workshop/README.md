# Redesigned Workshop GT evidence

Ten fixed-object position/presence variants. Each variant contains the validated GT action list, functional assignment, five-camera scene snapshot, and physical execution summary. Old 14-variant evidence was retired. New MP4 rendering is intentionally separate from the logical and physical validation run.
